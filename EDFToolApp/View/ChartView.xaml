<UserControl x:Class="EDFToolApp.View.ChartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:chart="clr-namespace:EDFToolApp.Chart"
             xmlns:local="clr-namespace:EDFToolApp.View" 
             xmlns:vm="clr-namespace:EDFToolApp.ViewModel" 
             d:DataContext="{d:DesignInstance Type=vm:ChartViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <chart:CartesianChartView
        Title="{Binding Title}"
        XAxes="{Binding XAxes}"
        YAxes="{Binding YAxes}"
        Series="{Binding Series}"/>
</UserControl>
